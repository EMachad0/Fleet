# Block `bun test` — this project uses Vitest (`bun run test`).
#
# `root` scopes Bun's runner so it never picks up Playwright specs.
# `preload` prints a helpful error and exits 1 before any test runs.

[test]
root = "src/convex"
preload = "./scripts/no-bun-test.ts"
